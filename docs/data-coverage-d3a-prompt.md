# D3a bounded history foundation — executed offline September 16, 2026

Delivery and remaining constraints: [D3a report](data-coverage-d3a-report.md). The instructions below are preserved as the executed scope.

Implement the minimum offline D3a segmented-history foundation in `/Users/michaelfuscoletti/Desktop/prediction-arb`, following `docs/data-coverage-d2-capacity-decision.md`, the current tracker/plan, architecture/SSOT and repository instructions. Preserve all existing code work, original journals and recovery indexes. D2 remains incomplete; prior live subscription/browser evidence keeps its original candidate identity.

Build on the existing file journal and replay/owner modules. Do not add a database, collector framework, scheduler, outcome/settlement feed, backfill, retention deletion or broad dashboard work.

Create an explicitly versioned, initially offline-only segmented mode. Keep existing D2 mode and all its lifetime limits unchanged. Do not reset the 2,048 logical-ingress allowance on rotation or count a batch as fewer logical observations. Distinguish logical observations, physical envelopes, encoded bytes, expanded bytes, pending queue work, durable acknowledgements and segment/control records. Any future live policy change must remain a separately reviewed proposal.

For the new isolated test mode only, use an explicit finite run budget of at most 4,096 logical ingress observations, four segments and 16 MiB total encoded ingress; retain the existing 256 MiB RSS, 48-object/4 MiB expanded queue, 128 MiB output reservation and 1 GiB disk-floor safeguards. This test policy is a deliberate offline experiment beyond the legacy D2 record count, not authorization to increase D2 live capacity. Roll segments before 1,024 logical ingress observations, 4 MiB encoded payload or 8 MiB expanded payload, whichever comes first, preserving 64 KiB/64 physical records of terminal/control headroom within existing per-journal ceilings. Include all manifest, seal and dependency data in physical/disk accounting; do not hide payloads in unbudgeted sidecars. If these proposed test bounds conflict with measured implementation needs, report the conflict rather than increasing them silently.

Preserve each admitted native observation, identity, receipt/source timestamp, order, health/gap transition and exact calculation input. Retain raw inputs; deterministic derived recipes are optional, versioned and must retain non-derivable clocks/identity plus exact replay proof. Prefer keeping existing lossless rows for this slice. Define cross-segment stream/snapshot/metadata dependencies explicitly, with bounded checkpoints or sequential replay. Never make a book usable from an unresolved dependency.

Implement crash-safe segment sealing, manifest publication, interrupted-tail handling and explicit new-session restart boundaries. Stop must finalize both feeds/tasks and the active segment without falsely declaring incomplete data complete. Original journal formats remain readable. Keep original source identity protections; do not normalize away the known legacy recovery device drift.

Validate with the real retained 575-book/packet capture split across meaningful stream/discovery boundaries and clearly labeled repeated-pressure fixtures. Preserve originals read-only and write derived output separately. Assert exact book/packet and saved-calculation equality, order and accounting across segments. Exercise missing, reordered and corrupt segments, partial writes/fsync failure, interruption at every rotation publication boundary, pending queue/rejection accounting, resource stops, manual Stop and finalization. Test fresh old/new recovery fixtures on the current device. Report the two legacy-index environmental failures separately; do not call the full suite passing while they fail.

Measure incremental replay RSS/working set, bytes, logical/physical counts, queue peaks, throughput and disk growth across multiple segments. Do not materialize the whole segmented run to claim bounded replay. Synthetic repetitions are pressure tests, not additional real history or live-duration predictions. Explain which cumulative constraints would still limit daily use, including REST/message/connection budgets and disk policy; do not enable daily collection in this slice.

Deliver code, focused tests, exact candidate/evidence identity and a concise D3a report. Update the tracker/plan without declaring D2 or full D3 complete. Keep remaining D2 refresh/manual-Stop live checks distinct from sustained-capacity qualification. Prepare a later validation proposal only if the evidence supports it; do not run it.

Authorized by this next prompt only when separately invoked: local implementation, offline replay/isolated fixtures, derived evidence and documentation. No credentials, venue requests, live collection, beta restart, attempt reset, original-evidence edits/deletion, database migration, trading, commits, pushes or publishing. Stop after D3a offline delivery.
