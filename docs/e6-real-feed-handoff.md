# E6 next slice — prediction-market feeds first

Updated September 15, 2026 following owner correction. Prediction-market data is the core; sportsbooks are ancillary. The Odds API is not required for this slice. Existing reference/pricing implementation and all historical evidence remain preserved.

## Authorized slice scope (implementation and results now recorded)

1. Make reference inputs optional in run specification, preflight, session ownership and health. Do not create a fake healthy reference or synthetic estimate. Missing references only disable dependent calculations.
2. Wire the existing Kalshi and Polymarket US authenticated read-only producers to E6 with explicit Start, bounded discovery, cancellation and native resynchronization. Preserve startup-idle behavior and original owner services.
3. Reuse existing project credential references through their supported local mechanism; never ask for keys in chat. Verify current official access/rate conditions before requests. No signup, subscription or additional spending.
4. Engineer discovers exact IDs for one overlapping NFL pregame moneyline, records participant/schedule mappings and capture scope. Choose the earliest unambiguous common event allowing the full bounded run before kickoff. Unresolved settlement blocks qualified economics, not otherwise authorized raw capture.
5. Run one at-most-300-second read-only capture, using finite request/connection/frame/byte budgets and a new isolated file-journal output. Verify manual Stop or deadline, truthful coverage and exact native observation replay. Keep unavailable economics explicit; no synthetic probabilities or fees enter real mode.
6. Record candidate, source identities, counts, gaps, replay and limits; preserve exports, stop isolated resources, update tracker. This proves only the measured bounded behavior, not continuous reliability or full E6.

The owner invoked this slice on September 15. Prediction-only implementation, bounded discovery and the one-session result are recorded in [the slice report](e6-prediction-only-report.md). Further collection requires a new bounded scope; the completed run does not enable unattended operation.

No orders, account mutation, owner database/service changes, unattended collection, commits, push or publishing. E5 basic review remains accepted; no exhaustive owner walkthrough. Strict 250 ms qualification remains deferred.
