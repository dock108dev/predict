# Historical D2 finite validation handoff — executed September 16

The authorized execution is recorded in [validation report](data-coverage-d2-validation-report.md). The later [offline efficiency repair](data-coverage-d2-journal-efficiency-report.md) does not establish capacity for another run. This historical handoff is not a current recommendation or authorization.

This handoff confers no live authorization. Request a separate authorization for one finite attempt, existing credential references and read-only venues, plus an idle-only beta restart if required. Do not start or restart an active collector. No attempt reset, automatic retry, resource increase or D3 work.

Candidate: [offline repair identity and checks](data-coverage-d2-offline-repair-report.md), with exact source hashes in [verification](../evidence/d2-coverage/offline-repair-20260916/verification.json). Verify hashes and assess every difference before live access. Reuse unchanged checks; rerun affected checks. Confirm beta idle, feeds stopped and shared collector lock free. Create a uniquely named attempt through the existing `CoverageOwner(pilot_output=...)` mechanism, retaining the shared lock and all consumed attempt records. Do not reuse a consumed directory. Any required restart must remain idle until explicit Start.

Carry forward replacement session `97dbc1e0-36c7-49c8-a19b-f4856278e21c`: 32 Kalshi events/64 markets, all 64 requested/acknowledged/receiving/ever usable, exact 491-book replay, first scheduled refresh, and demonstrated browser closure independence. Those are historical observations for the prior candidate, not new live qualification of this repair. Repeating browser closure is not the remaining target.

## Finite operation and direct Stop

1. Before Start, have an independent loopback control ready and record a monotonic Start clock. The hard duration remains **180 seconds including discovery**, with all fresh venue attempts/retries charged inside that window. No credential/account/data requests before Start. Use existing unchanged query paths only.
2. Record independent native event/market counts, published generation/age, completeness and exclusions, then selected, requested, acknowledged, receiving and usable counts for each venue. US embedded listings must remain visible even if separate queries are empty. Document missing eligibility fields/conflicts individually; unknown full counts remain unknown. Keep Short depth unavailable.
3. Observe the first scheduled refresh at 60 seconds. Capture generation before/during/after and producer-applied generation separately. Confirm no transient partial denominator replaces the last completed generation; inspect response attempted/received/durable accounting. Do not force disconnects, closures or reschedules. Missing transition observations remain offline-only.
4. **At 90 seconds from Start, directly issue `POST http://127.0.0.1:8783/api/stop` with JSON `{}`, `Content-Type: application/json` and `Origin: http://127.0.0.1:8783`.** This is the existing explicit manual Stop route used by the button. Issue it from the independent local control even if the viewing tab is closed; do not wait for another user prompt, the refresh to finish, or missing evidence. Use short bounded status waits so the operator reaches this action on time. This leaves 90 seconds before the hard deadline for cleanup and verification. No scheduled/unattended collector or autoresume is introduced.
5. If a resource/failure stop occurs first, retain it, finalize and report manual Stop unverified. Do not launch another attempt. After the Stop action, verify both feeds, all collector tasks and finalizer stopped, queue accounting, terminal journal acknowledgement/manifest, zero usable books and shared lock release. Confirm the beta remains idle; no restart into collection.

Retain unchanged bounds: 100 REST attempts/venue including retries, at most 2 requests/second reduced by verified account budget; 100 market IDs/venue; six simultaneous sockets/twelve connection attempts. Kalshi groups 20 IDs, US 100; per-group connection/message caps remain. Ten-page traversals, Kalshi 200 rows/page and US five; 1 MiB body/frame; 16 MiB native bytes/venue; **16 MiB/2,048 serialized ingress records**; 256 MiB RSS; 48-record/4 MiB queue; 128 MiB output; 1 GiB disk floor; 32 MiB/4,096 journal with 64 KiB/64 terminal reserve. Respect lower verified limits. Do not extend to obtain missing evidence or claim rotation as simultaneous coverage.

Report exact candidate/session identity, actual timing and direct Stop request/response timestamps, generation/age/refresh outcome, denominators and unknowns, all subscription stages and exclusions, resources/gaps, journal finalization and read-only prior-evidence hash comparison. A resource stop or unresolved required evidence keeps D2 incomplete. US partial coverage must stay explicit even if supported embedded subscriptions succeed. Stop after D2 validation; D3 requires its own task.
