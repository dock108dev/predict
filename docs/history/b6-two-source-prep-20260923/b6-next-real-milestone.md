# Next real-data milestone — proposed, not authorized

Prepared September 23 UTC / September 22 local, after finite B6 offline work.
This is a concrete partial qualification proposal, not permission to collect.
It does not replace the pending [four-venue B3 proposal](b3-native-qualification-proposal.md).

## Available-access basis

Current retained records identify prior Kalshi and Polymarket US read-only access.
No credentials were accessed or tested in B6. Novig/ProphetX requests remain
pending in current records. The real Pinnacle NFL sample is historical; its one
credit is consumed. No usable independent model has been acquired.

## One bounded milestone

Propose one **90-second**, two-venue, pregame NFL full-game winner observation
through ordinary Predict. This narrower next step uses the already recorded
access paths to qualify exact native identity, purchase units, rule applicability,
fees, freshness, Stop and saved replay on the current implementation. It is not
four-source or six-sport beta qualification. Zero or negative comparisons pass
when the original-input math is correct; unavailable comparisons require exact
reasons and leave the corresponding qualification open.

Before approval, freeze one shared event's participants, scheduled UTC start,
Kalshi market/series and US game/market IDs from retained records or an explicitly
included bounded discovery stage. If no current compatible event can be selected
inside that stage, stop; do not extend scope or guess identity. Retain unknown
terms/fees/depth as unavailable. A fixture rule descriptor cannot approve a real
contract. Include exact source identity, contract text and fee-version review in
the acceptance record.

Use the existing isolated native launcher, idle until explicit Start, with a
fresh run UUID, exact app/spec hashes and one 15-minute UTC start window supplied
at approval time. Consume once before access. Stop at approximately 60 seconds
when evidence is sufficient; 90 seconds is the hard deadline including discovery.
No retry attempt, quota increase, existing-beta restart or automatic extension.

| Source | Recorded local reference | Read-only scope |
|---|---|---|
| Kalshi | `keychain:prediction-arb.kalshi.production/market-data` | Existing approved-host discovery/account-limit and market-data operations only; `KXNFLGAME`, one selected event, at most two winner contracts |
| Polymarket US | `keychain:prediction-arb.polymarket-us/retail-api` | Existing approved-host event/market discovery and market-data subscription only; NFL, same event, at most two winner contracts |

Preserve existing source policies: at most 100 REST attempts per venue including
retries; six concurrent sockets/twelve connection attempts globally, with this
scope using at most one group per venue and two connections per group. Queue
48 records/4 MiB, ingress 2,048 records/16 MiB, journal 32 MiB, output 128 MiB,
collector peak RSS 256 MiB. Freeze the exact implemented limits in the spec;
if discovery requires more, report the insufficient bound rather than raising it.
All unused native/reference roles remain disabled and perform no secret lookup.

Retain native originals, observed versus exchange times, usable/selected/excluded
counts, discovery completeness, gaps/recovery, independent unit/fee/payout math,
immutable pregame cutoff, terminal/cleanup evidence and fresh-process replay.
An unavailable required input fails only that qualification claim; it is not
silently substituted or declared absent from beta scope.

## References and subsequent work

Do not merge the old Pinnacle sample with new books as contemporaneous EV. It
remains separately browsable historical evidence. A new Pinnacle request needs
its own explicit request/credit budget and exact event/market scope; an independent
model needs a selected accessible source, original dated probabilities and
compatible outcome/rule bindings. Those approvals are not bundled here.

When Novig/ProphetX provisioning arrives, reconcile API type, production tournament,
units, native rules and request-cost schedule before freezing the existing
four-venue proposal. The offline Novig workload exhausted its unchanged 30-call
allowance near 150 seconds; budget the actual endpoints explicitly before any
longer run. Do not infer provider latency or reliability from fixtures.

Next actor: engineering prepares the exact event/spec/window package from owner-
supplied access references and then requests one approval; the owner approves
that concrete package before new access. There is no owner beta acceptance request.
