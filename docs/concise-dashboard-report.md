# Concise cross-venue dashboard

Completed September 15, 2026 EDT (September 16 UTC). [Open beta](http://127.0.0.1:8783/). Left open and idle on the original six-game real capture, session `5c9b7dca-a813-4d77-80f5-0691d06068eb`.

The ordinary dashboard shows **12 cross-venue pairs**, including six unavailable alternatives. Game detail shows only its two cross-venue pairs. The six same-venue pairs remain in the unchanged calculation API and regression oracle. Primary content is game, actual purchases/top prices, conditional net dollars and modeled size. ROI, fee/depth explanations, cashflows and provenance are behind Details. Filters/sorting remain available in a disclosure; requested size, Start/Stop, bounds, saved selection and return state remain intact. Material unavailable reasons, saved/current status and capture time remain visible. The EV tool is explicitly a user-entered what-if with event/outcome-bound assumptions and no estimated fair probability.

**Verification:** 23 focused Python tests passed, including repeatable local-producer Start/Stop, bounds, overlap rejection, saved/current separation and restart. Presentation checks cover 12/6 visibility, unavailable and negative/zero handling, and event/outcome assumption isolation. JavaScript syntax checks passed. The independent oracle's **18 Arb candidates and 96 EV scenarios remain byte-identical**. No math, source observations, fee versions or historical assessment basis changed. The dashboard API matches before/after except its preexisting per-request fee-audit calculation timestamp and derived context hash.

**Actual browser:** selected an older saved scan and returned to the six-game scan; changed dollar sorting; opened inline Details without navigation; entered game detail and returned with capture, size and sort retained. Dashboard and detail fit 390×844 with 390px document width; detail showed two pairs. Two browser-driven local-producer scans (two and three games) completed Start → Stop → saved, displayed four/six cross-venue pairs and allowed saved switching. An explicit local-test EV input cleared on another outcome and restored on return. No browser warnings/errors. Temporary viewport reset; local test process stopped. The real beta was never started collecting.

All **1,670 preexisting evidence files** remain byte-identical. Existing uncommitted work and captures are preserved. Only the identified idle opportunity-board beta process was restarted, after confirming collection idle and cleanup complete. No real collection, credentials, new feeds, paid access, database changes, unrelated restarts, trading, commit, push or publishing.

[Verification and implementation hashes](../evidence/concise-dashboard/verification.json) · [test output](../evidence/concise-dashboard/focused-tests.txt) · [presentation checks](../evidence/concise-dashboard/presentation-tests.txt). Arithmetic remains conditional on the existing fee and normal-winner assumptions; negative, zero and unavailable are valid. This is UI verification, not owner acceptance or execution qualification.

Stop here. The next task will scope one NFL pregame moneyline EV research loop using existing reference/fair-price components. It is not implemented by this slice.

## Completion recheck

The requested slice was already present in the working tree. Reverified its exact recorded implementation hashes without replacing existing edits. All 23 focused tests, presentation checks and JavaScript syntax checks pass again; the independent 18-candidate/96-scenario baseline remains byte-identical. The local-producer lifecycle test completed two Start/Stop scans in temporary storage. A fresh actual-browser walkthrough switched saved scans, entered game detail and returned; the baseline shows 12 pairs and detail two, with both layouts fitting 390px. No browser warnings/errors. Restarted only the identified beta after confirming idle collection and completed cleanup; left it open on saved real results. No additional UI or math changes were needed. [Recheck evidence](../evidence/concise-dashboard/recheck-20260916/verification.json).
