# One finite five-minute live validation — later authorization required

**Prepared proposal only. No live run is authorized or started by this document.** The [offline qualification](data-coverage-supervised-5m-qualification-report.md) passes on unchanged application identity `f94f5c9b502eca78cc91af3a37f69445a8d3f40ed703d57adf18bb9f90dbf020`; final application/test candidate is `39017e90b75b0b2ade6fe91ef4bbc9b646c186f87adf8ce470937877976f31b9`. Read its exact gate provenance, the frozen policy, tracker/plan and applicable repository instructions first.

The current candidate deliberately permits `predict-supervised-segmented-5m-v1` only through isolated offline fixtures. It is **not a ready-to-run live command**. Later authorization must explicitly cover an isolated supervised live entry point and one live session. Do not pass real endpoints/credentials through the mock bypass or silently widen production defaults. If a minimal live entry is implemented, preserve the existing owner/collector/journal/replay, rerun affected offline guards and freeze a fresh reviewed candidate before any live Start. A failed guard or required behavior change stops before venue access.

## Proposed authorization scope

One owner-started, supervised read-only session, no autoresume, fresh exclusive output directory and new session identity. Preserve all prior consumed attempts and evidence; do not reset or reuse them. Keep the existing beta stopped/unchanged and reject conflicting collector ownership rather than overriding its lock. Use only the established project credential references and allowed Kalshi/Polymarket US endpoints; never print or retain secrets. No trading, database work, commits, pushes, publishing or automatic restart.

Use the unchanged frozen five-minute profile and its joint budgets: maximum 300 seconds from Start including discovery; initial discovery plus one refresh at 120 seconds; owner directly presses Stop at 240 seconds. The 300-second deadline remains the safety cutoff if manual Stop is not delivered. Do not claim manual Stop validation in that situation, and do not run a replacement session under this authorization.

The observed 64 Kalshi/32 US markets are the comparison baseline, not a guaranteed current universe. Report discovered additions, exclusions, selections and generation changes within the frozen catalog/market/stream/request caps. Keep US full-market completeness unknown. Current account pacing can further constrain the session; do not raise budgets to reach a target. No fixture workload or synthetic prices enter the live journal.

Before Start, verify the exact candidate, idle owner, no conflicting session, time/schedule eligibility, resource reservation and explicit Stop availability. Keep 1 GiB free-disk floor plus remaining output reservation; output 224 MiB with 32 MiB finalization headroom; RSS 256 MiB hard/224 MiB intake stop and 192 MiB qualification target. Preserve all other frozen record/byte/rate/request/connection/queue/state bounds and cumulative accounting across rotation. Do not delete evidence to make room.

## Required evidence from this one session

Record actual Start, refresh, direct Stop and deadline clocks; per-venue request attempts/timestamps/pacing/retries; published/applied generations and additions/exclusions; requested/acknowledged/receiving/usable markets; stream groups/connections/churn; actual workload/payload volume. Report resource peaks separately for collection and finalization, all record/byte/output budgets and remaining headroom.

After Stop or any resource/error cutoff, close and join tasks/feeds, drain admitted work, retain incomplete evidence honestly, and perform exact incremental identity/clock/book/packet replay within the frozen 300-second finalization ceiling (qualification target 240 seconds). Require received = accepted + rejected, accepted = durable = drained, zero unresolved/pending/durable-not-queued for a successful completion. Report actual closure latency against 5 seconds, finalization time, native exactness and consumed-attempt protection. Verify all preexisting evidence unchanged.

A resource Stop, incomplete discovery, failed publication/replay, excess memory or missed direct Stop is a reported limitation/failure, not permission to retry, expand limits, sample updates or manufacture completion. Preserve its evidence and stop. Passing this single live validation still establishes neither thirty-minute/daily capacity nor full D3; outcome/settlement linkage and the other D3 work remain separate.

Deliver one concise live-validation report with the frozen candidate/session identity and update tracker/plan. Preserve the two environmental recovery findings, prior memory failures and test-harness failures as separate historical records. Stop after the one session and its finalization/report.
