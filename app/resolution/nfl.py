"""Compatibility exports for the original NFL resolution record contract."""
from app.resolution.core import (VERSIONS, VENUES, MAX_RECORDS, record, validate,
    emit_records, target, binding_error, value_error, lineage, history, group,
    expectation, resolve)
