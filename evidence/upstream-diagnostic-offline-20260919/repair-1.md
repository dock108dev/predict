# First required failure and bounded repair

Focused run 1 stopped at test 6. The fixture title used `Detroit at Buffalo`,
whereas the existing series-gated native participant parser requires `vs`.
The new selector correctly rejected unresolved identity. Changed only the
synthetic fixture title to the retained native parser's documented shape.
No eligibility rule or assertion was weakened. Original failure log retained.
Zero major lifecycle runs consumed. Next: a fresh focused run within 180 seconds.
